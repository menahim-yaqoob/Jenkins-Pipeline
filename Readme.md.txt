# Apache2 Jenkins Pipeline 🚀

This repository demonstrates how to use **Jenkins Pipeline** to build and run an **Apache2 web server** inside a Docker container.

---

## 📂 Repo Structure
├── Dockerfile # Builds Apache2 Docker image
├── Jenkinsfile # Jenkins pipeline in Groovy
├── jenkins-pipeline.yml # Jenkins pipeline in YAML
└── README.md # Documentation